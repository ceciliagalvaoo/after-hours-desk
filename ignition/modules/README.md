# ignition/modules/

Empty on purpose — Phase 0 (toolchain scaffold) only.

Hardhat Ignition deployment modules for Sepolia land here from Phase 2/6
onward (cUSDC wrapper, AfterHoursDesk, price-reference contract), following
`nox-product-poc`'s (cVault) `ConfidentialVaultFactory.ts` pattern — CREATE2
where useful, auto-verify on Etherscan via `--verify`.
